<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlhdlh.mapper.MembersMapper">

	<!-- 로그인 체크 -->
	<select id="LoginCheck" parameterType="com.dlhdlh.dto.MembersDto" resultType="int">
		<![CDATA[ 
			SELECT count(user_id) 
			FROM Members 
			WHERE user_id = #{userId} and user_pw = #{userPw}
		]]>
	</select>

	<!-- 멤버 정보 조회-->
	<select id="SelectMemberInfo" parameterType="String" resultType="com.dlhdlh.dto.MembersDto">
		<![CDATA[ 
			SELECT user_id, user_name , authority 
			FROM Members
			WHERE user_id = #{userId};
		]]>
	</select>
	
	<!-- 회원가입-->
	<insert id="InsertMember" parameterType="com.dlhdlh.dto.MembersDto">
		<![CDATA[ 
			INSERT INTO Members (user_id, user_pw, user_name, signin_dt)
			VALUES ( #{userId}, #{userPw}, #{userName}, now());
		]]>
	</insert>
	<insert id="InsertPersetCust" parameterType="com.dlhdlh.dto.MembersDto">
		<![CDATA[ 
			INSERT INTO PersetCust (user_id)
			VALUES ( #{userId});
		]]>
	</insert>
	<insert id="InsertPersetWorkLog" parameterType="com.dlhdlh.dto.MembersDto">
		<![CDATA[ 
			INSERT INTO PersetWorkLog (user_id)
			VALUES ( #{userId});
		]]>
	</insert>
	
	<!-- Id 체크 -->
	<select id="UserIdCheck" parameterType="com.dlhdlh.dto.MembersDto" resultType="int">
		<![CDATA[ 
			SELECT count(user_id) 
			FROM Members 
			WHERE user_id = #{userId}
		]]>
	</select>
	
	<!-- 권한 체크 -->
	<select id="ManagerCheck" resultType="com.dlhdlh.dto.MembersDto" parameterType="String">
		<![CDATA[ 
			SELECT user_id, authority 
			FROM Members 
			WHERE user_id = #{userId}
		]]>
	</select>
	
	<!-- 유저 목록 -->
	<select id="UserList" resultType="com.dlhdlh.dto.MembersDto" parameterType="com.dlhdlh.dto.MembersDto">
		<![CDATA[ 
			SELECT *
			FROM Members 
			WHERE user_id like concat('%', #{userId}, '%') and user_name like concat('%', #{userName}, '%')
		]]>
	</select>
	
	

</mapper>